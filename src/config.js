// comet.is application configuration variables

// Config will be calculated from files in the config/ directory and included
// statically here.

// Wrap the static BUILD_ENV variable here so it's only included in-code once.

const cometConfig = BUILD_ENV;

export default cometConfig;
